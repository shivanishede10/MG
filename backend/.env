# ============================================================
#  MediGlow — Backend Environment Variables
# ============================================================
# IMPORTANT: NEVER commit this file to version control.
# It contains secrets. Your .gitignore should include .env

# --- Server ---
PORT=5000

# --- JWT ---
# Change this to a long random string in production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=mediglow_super_secret_key_change_this_in_production

# --- Email (Nodemailer / Gmail) ---
EMAIL_USER=shivanishede69@gmail.com
EMAIL_PASS=ktmjzgsvtzwcnsxr
EMAIL_FROM="MediGlow App" <mediglowapp@gmail.com>
